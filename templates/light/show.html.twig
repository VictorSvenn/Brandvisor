{% extends 'base.html.twig' %}

{% block title %}Light{% endblock %}

{% block body %}
    <h1>Light</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ light.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ light.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ light.image }}</td>
            </tr>
            <tr>
                <th>Desciption</th>
                <td>{{ light.desciption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('light_index') }}">back to list</a>

    <a href="{{ path('light_edit', {'id': light.id}) }}">edit</a>

    {{ include('light/_delete_form.html.twig') }}
{% endblock %}
