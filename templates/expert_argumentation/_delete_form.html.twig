<form method="post" action="{{ path('expert_argumentation_delete', {'id': expert_argumentation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expert_argumentation.id) }}">
    <button class="btn">Delete</button>
</form>
