<form method="post" action="{{ path('recommandation_delete', {'id': recommandation.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recommandation.id) }}">
    <button class="btn">Supprimer</button>
</form>
