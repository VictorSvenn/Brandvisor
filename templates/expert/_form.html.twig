{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="mx-auto col-3">
    <button class="btn mx-auto btn-success text-white">{{ button_label|default('Enregistrer') }}</button>
</div> {{ form_end(form) }}