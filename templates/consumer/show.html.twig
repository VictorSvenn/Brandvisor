{% extends 'base.html.twig' %}

{% block title %}Consumer{% endblock %}

{% block body %}
    <h1>Consumer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consumer.id }}</td>
            </tr>
            <tr>
                <th>GeographicArea</th>
                <td>{{ consumer.geographicArea }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ consumer.birthDate ? consumer.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consumer_index') }}">back to list</a>

    <a href="{{ path('consumer_edit', {'id': consumer.id}) }}">edit</a>

    {{ include('consumer/_delete_form.html.twig') }}
{% endblock %}
