{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<div class="text-center p-5">
    <h1>Espace Administrateur</h1>
</div>

<div class="container-fluid mb-5">
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('validate_opinions') }}">✅  Validation des avis</a>
    </nav>
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('news_new') }}">📰  Déposer une news</a>
    </nav>
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('recruitment_new') }}">👔  Déposer une offre d'emploi</a>
    </nav>
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('init_validate') }}">💡  Validation des initiatives</a>
    </nav>
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('engagements_validate') }}">🏅  Validation des engagements<br />
        <p class="h6 text-center ">( pour la 4e étoile )</p></a>
    </nav>
</div>

{% endblock %}
